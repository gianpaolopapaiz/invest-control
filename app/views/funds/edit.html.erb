<div class="app-form">
  <h2 class='text-center'>Editar Fundo</h2>
  <p class='text-center w-75'><strong><%= @fund.short_name %></strong></p>

  <%= simple_form_for @fund do |f| %>
    <%= f.input :strategy, label: 'Estratégia', collection: Fund::STRATEGIES %>
    <%= f.input :buy_date, label: 'Data de entrada' %>
    <%= f.input :buy_quantity, label: 'Quantidade' %>
    <%= f.input :buy_price, label: 'Preço de entrada' %>
    <%= f.input :advisor, collection: Stock::ADVISORS %>
    <div class="d-flex justify-content-center align-items-center w-100 mt-3">
      <%= link_to 'Voltar', portfolio_path(@fund.portfolio), class: 'btn btn-secondary btn-block w-50 m-0 mr-1' %>
      <%= f.submit 'Atualizar', class: 'btn btn-primary btn-block w-50 m-0 ml-1' %>
    </div>
  <% end %>

</div>